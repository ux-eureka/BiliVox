## 修复计划：系统日志可见性与右侧面板布局

### 1. 诊断系统实时日志不可见问题

**修改** `frontend/src/views/ControlPanel.vue`:
- 为 SystemLogs 组件添加 `height="100%"` 属性
- 确保 t-card 内部使用 flex: 1 正确传递高度
- 验证 t-card__body 正确继承父容器高度

### 2. 修复右侧两卡片布局

**修改** `frontend/src/views/ControlPanel.vue`:
- 右侧 side-panel 添加 `display: flex; flex-direction: column; gap: 12px;`
- task-panel 添加 `flex: 0 0 calc(50% - 6px);`（50%高度减去一半间距）
- activity-panel 添加 `flex: 1 1 calc(50% - 6px);` 或保持 `flex: 1`
- 移除 `panel-divider`，使用 gap 控制间距
- 统一使用 TDesign token：水平间距 16px、垂直间距 12px
- 添加 `overflow: hidden` 防止滚动溢出

### 3. 确保响应式适配

- 1366×768 分辨率下验证布局一致性
- 2560×1440 分辨率下验证布局一致性
- 添加 `flex-wrap` 或媒体查询应对不同视口

### 4. 验证与交付

- 验证日志组件正确显示
- 验证两卡片各占50%高度
- 验证间距符合规范（水平16px、垂直12px）
- 无滚动冲突、无溢出