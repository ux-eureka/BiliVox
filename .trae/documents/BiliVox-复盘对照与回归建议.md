## 复盘输出物
- 生成一份“原始文档需求 → 当前实现”对照表（按模块：下载/ASR/LLM/增量更新/清理/配置/进度/前后端/监控），每条标注：已实现｜优化改动｜部分实现｜已放弃，并给出代码定位。
- 生成一份“迭代原因归因”说明：哪些属于产品化增强（UI/服务化/历史/任务管理），哪些属于外部约束（B 站风控 352/-799）导致的权衡。
- 给出“是否需要拉回原始文档逻辑”的决策建议：必须回归项、建议保持现状项、可选增强项。

## 对照分析方法
- 以你提供的原始文档条目为基准，逐条映射到现有文件与接口：
  - CLI 路线：main.py + core/*
  - 产品路线：backend/* + frontend/*
- 对每条需求核验 3 个维度：
  - 行为一致性（功能是否等价）
  - 运维/体验差异（是否被 UI/服务化替代）
  - 约束与风险（是否因反爬/性能/稳定性改写）

## 当前已观察到的关键差异（将写入对照表）
- 从“纯 CLI 批处理”扩展为“FastAPI + Vue 产品形态”，CLI 仍保留但不再是唯一入口。
- 增量更新的“跳过已处理”仍在，但判定策略从“严格 BV 文件存在”演化为“bv_id 或标题匹配 + history 绑定 taskId”，更偏产品侧可用性。
- 进度展示从 tqdm 控制台转为“后端 stage progress + 前端队列/估算”的混合模式。
- 反爬约束显著：拉 UP 投稿列表经常触发 352/-799，导致 UI 侧“识别失败”；当前代码已加入 cookies 支持与更明确错误提示，但默认配置未显式引导。

## 回归/不回归建议（初稿方向）
- 建议保持：服务化 + UI（这是超出原文档但显著提升可用性的优化，不建议倒退）。
- 建议回归/补齐：把 cookies 相关配置正式纳入 config.yaml 与 UI，作为“识别 UP”前置条件；同时让错误信息前端可读（中文乱码/编码）并给出操作指引。
- 可选增强：把“增量更新判定”从标题模糊匹配收敛到更稳定的 bv_id 元数据索引（减少误判）。

## 下一步（你确认后我会执行）
- 在仓库新增一份 Markdown 复盘文档（对照表 + 归因 + 建议）。
- 若你选择“拉回部分原始逻辑”，再按建议项逐个落地改动（配置项、UI 引导、增量判定、错误编码等），并配套回归测试用例。